# LLM provider
# DESIGN.md doesn't name a specific provider — defaulting to Anthropic since
# this project runs inside a Claude Code environment. Confirm/update if a
# different provider is actually used, and log the choice in DESIGN.md.
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Scryfall and EDHREC require no API keys — omitted here.
